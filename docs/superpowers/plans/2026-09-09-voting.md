# 투표 기능 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 답변이 공개된 뒤 참가자들이 제일 마음에 드는 답 하나에 투표하고, 전원이 투표하면 득표 수가 다 같이 공개되게 한다.

**Architecture:** 라운드 상태를 늘리지 않는다. 라운드는 `revealed` 인 채로 투표 단계를 거치고, 새 `votes` 테이블과 `rounds.voting_closed_at` 컬럼이 진행 상태를 담는다. 결과 공개 연출은 답변 공개에 쓰는 `revealTimings` 를 그대로 재사용한다. 작성 화면의 블러는 서버가 답변 길이만 보내고 프론트가 가짜 블록을 그린다 — 실제 텍스트는 클라이언트에 도달하지 않는다.

**Tech Stack:** NestJS 11 · TypeORM 0.3 · PostgreSQL (백엔드) / Next.js 16 · TanStack Query 5 · motion 13 (프론트)

**Spec:** `docs/superpowers/specs/2026-09-09-voting-design.md`

## Global Constraints

**레포가 둘이다.** Task 1-5 는 백엔드(`~/Desktop/quiz-app`), Task 6-9 는 프론트엔드(`~/Desktop/quiz-app-web`). 각 태스크에 명시돼 있다.

**두 가지 유출 방지가 이 기능의 핵심이다. 어느 쪽도 타협하지 않는다.**

1. **블러는 가짜여야 한다.** CSS `filter: blur()` 로 실제 텍스트를 가리는 구현은 금지. 텍스트가 DOM 에 있으면 개발자도구로 읽힌다. 서버는 `answerLength: number | null` 만 보내고 프론트는 그 길이만큼 가짜 블록을 그린다.
2. **투표 중에는 득표 수를 보내지 않는다.** `answers[].voteCount` 는 `voting_closed_at` 이 찍히기 전까지 `null`. 실시간 집계가 보이면 앞서는 답에 표가 쏠린다.

**기존 불변식을 깨지 않는다:** 공개 전까지 남의 답변 텍스트는 어떤 경로로도 클라이언트에 도달하지 않는다.

**게임 엔티티에 TypeORM 관계 데코레이터를 쓰지 않는다.** 조인은 raw SQL 이나 명시적 레포지토리 호출로 한다.

**임포트 규칙:** 모듈 경계를 넘으면 `src/...` 별칭, 같은 모듈은 상대 경로.

**모든 사용자 노출 문구는 한국어.** 기존 서비스 메시지와 같은 어조(`이미 제출했습니다. 수정할 수 없습니다`, `이 방의 참가자가 아닙니다`).

**커밋 메시지는 한국어와 영어만, 한자 금지.** 다음 두 트레일러로 끝낸다:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpXfupFmRo2repLqi8Pjo5
```

**푸시하지 않는다.** 커밋까지만 하고 보고한다.

**개발 환경:** 백엔드는 `PORT=3001 npm run start:dev`, 프론트 dev 서버가 3000. 로컬 Postgres 는 `postgresql://postgres:quizapp@localhost:55432/quiz_app` 이고 **실제 질문 은행이 들어있다** — 수동 테스트로 만든 방·참가자·라운드·답변·투표·`question_stats` 행은 전부 지운다(`rooms.host_participant_id` 를 먼저 NULL 로).

## 이 계획서의 상세도에 대해

백엔드 태스크(1~5)와 프론트의 순수 로직(6~7)은 **완전한 코드와 테스트**를 담고 있다.
이 기능의 correctness 가 거기 있다 — 투표 잠금이 틀리면 두 표가 들어가고, `voteCount`
게이팅이 틀리면 표가 쏠린다.

화면 태스크(8~9)는 **props 계약·동작 요구사항·호출할 API** 를 정하되 시각적 선택은
구현자에게 맡긴다. 누락이 아니라 의도다. 대신 **무엇이 참이어야 하는지**는 빠짐없이
적었다 — 어떤 상태에서 무엇이 보이고 무엇이 보이면 안 되는지.

리뷰어는 화면 태스크에서 "코드가 없다"를 결함으로 보지 말고, 동작 요구사항이
지켜졌는지로 판단할 것.

**스킵된 라운드에는 투표가 없다.** `Skipped.tsx` 는 투표 props 를 받지 않으므로
구조적으로 불가능하다 — 별도 태스크가 없는 이유다.

---

## 파일 구조

**백엔드** (`~/Desktop/quiz-app`)

| 파일 | 책임 |
|---|---|
| `src/infrastructure/database/migrations/<ts>-AddVotes.ts` | votes 테이블 + rounds.voting_closed_at |
| `src/modules/game/entities/vote.entity.ts` | Vote 엔티티 |
| `src/modules/game/entities/round.entity.ts` | `votingClosedAt` 컬럼 추가 |
| `src/modules/game/vote.service.ts` | 투표 저장, 완료 판정, 강제 종료, 집계 |
| `src/modules/game/round.service.ts` | `listAnswers` 가 답변 길이도 제공하도록 보강 |
| `src/modules/game/dto/round.dto.ts` | 응답 DTO 확장 |
| `src/modules/game/dto/vote.dto.ts` | 투표 요청 DTO |
| `src/modules/game/game.controller.ts` | 투표 엔드포인트 2개, 응답 조립 |
| `src/modules/game/game.module.ts` | Vote 엔티티, VoteService 등록 |

**프론트엔드** (`~/Desktop/quiz-app-web`)

| 파일 | 책임 |
|---|---|
| `lib/types.ts` | 응답 타입 확장 |
| `components/game/BlurredAnswer.tsx` | 길이만 받아 가짜 블록을 그린다 |
| `components/game/AnswerForm.tsx` | 블러 목록 추가 |
| `components/game/Waiting.tsx` | 블러 목록 추가 |
| `components/game/Reveal.tsx` | 투표 전 / 투표함 / 결과 세 모습 |
| `app/room/[code]/page.tsx` | 투표 뮤테이션 배선 |

---

## Task 1: votes 테이블과 엔티티

**레포:** `~/Desktop/quiz-app`

**Files:**
- Create: `src/infrastructure/database/migrations/<timestamp>-AddVotes.ts`
- Create: `src/modules/game/entities/vote.entity.ts`
- Modify: `src/modules/game/entities/round.entity.ts`
- Modify: `src/modules/game/game.module.ts`
- Test: `src/modules/game/entities/vote.entity.spec.ts`

**Interfaces:**
- Produces: `Vote` 엔티티 (`id, roundId, voterParticipantId, answerId, createdAt`), `Round.votingClosedAt: Date | null`

- [ ] **Step 1: Vote 엔티티**

`src/modules/game/entities/vote.entity.ts`:

```typescript
import { Column, CreateDateColumn, Entity, Index, PrimaryGeneratedColumn, Unique } from 'typeorm';

@Entity('votes')
@Unique(['roundId', 'voterParticipantId'])
@Index(['roundId'])
export class Vote {
  @PrimaryGeneratedColumn({ type: 'bigint' })
  id!: string;

  @Column({ name: 'round_id', type: 'bigint' })
  roundId!: string;

  // 누가 던진 표인지. 결과 화면에는 절대 나가지 않는다 — 득표 수만 공개한다.
  @Column({ name: 'voter_participant_id', type: 'bigint' })
  voterParticipantId!: string;

  @Column({ name: 'answer_id', type: 'bigint' })
  answerId!: string;

  @CreateDateColumn({ name: 'created_at', type: 'timestamptz' })
  createdAt!: Date;
}
```

- [ ] **Step 2: Round 에 votingClosedAt 추가**

`src/modules/game/entities/round.entity.ts` 의 `revealedBy` 아래에 추가:

```typescript
  // 전원이 투표한 순간(또는 방장이 강제 종료한 순간). 결과 공개 연출의
  // 기준점이다 — revealedAt 과 같은 역할을 투표 결과에 대해 한다.
  @Column({ name: 'voting_closed_at', type: 'timestamptz', nullable: true })
  votingClosedAt!: Date | null;
```

- [ ] **Step 3: 마이그레이션 생성**

파일명의 타임스탬프는 `Date.now()` 값을 쓴다. 기존 마이그레이션(`1788878603550-AddOneOpenRoundPerRoomIndex.ts`)보다 커야 한다.

```typescript
import { MigrationInterface, QueryRunner } from "typeorm";

/**
 * 투표 테이블과 투표 종료 시각.
 *
 * UNIQUE(round_id, voter_participant_id) 가 1인 1표를 DB 수준에서 강제한다.
 * 애플리케이션 검사만으로는 동시 요청에서 두 표가 들어간다 — 답변 제출에서
 * 이미 같은 문제를 겪고 유니크 제약으로 막았다.
 *
 * voting_closed_at 은 결과 공개 연출의 기준점이다. 모든 클라이언트가 이
 * 시각을 기준으로 계산하므로 폴링 시점이 달라도 득표 수가 같은 순간에 열린다.
 */
export class AddVotes<TIMESTAMP> implements MigrationInterface {
    name = 'AddVotes<TIMESTAMP>'

    public async up(queryRunner: QueryRunner): Promise<void> {
        await queryRunner.query(`ALTER TABLE "rounds" ADD "voting_closed_at" TIMESTAMP WITH TIME ZONE`);
        await queryRunner.query(`CREATE TABLE "votes" ("id" BIGSERIAL NOT NULL, "round_id" bigint NOT NULL, "voter_participant_id" bigint NOT NULL, "answer_id" bigint NOT NULL, "created_at" TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(), CONSTRAINT "PK_votes" PRIMARY KEY ("id"))`);
        await queryRunner.query(`CREATE UNIQUE INDEX "uq_votes_round_voter" ON "votes" ("round_id", "voter_participant_id")`);
        await queryRunner.query(`CREATE INDEX "idx_votes_round" ON "votes" ("round_id")`);
        await queryRunner.query(`ALTER TABLE "votes" ADD CONSTRAINT "fk_votes_round" FOREIGN KEY ("round_id") REFERENCES "rounds"("id")`);
        await queryRunner.query(`ALTER TABLE "votes" ADD CONSTRAINT "fk_votes_voter" FOREIGN KEY ("voter_participant_id") REFERENCES "participants"("id")`);
        await queryRunner.query(`ALTER TABLE "votes" ADD CONSTRAINT "fk_votes_answer" FOREIGN KEY ("answer_id") REFERENCES "answers"("id")`);
    }

    public async down(queryRunner: QueryRunner): Promise<void> {
        await queryRunner.query(`ALTER TABLE "votes" DROP CONSTRAINT "fk_votes_answer"`);
        await queryRunner.query(`ALTER TABLE "votes" DROP CONSTRAINT "fk_votes_voter"`);
        await queryRunner.query(`ALTER TABLE "votes" DROP CONSTRAINT "fk_votes_round"`);
        await queryRunner.query(`DROP INDEX "idx_votes_round"`);
        await queryRunner.query(`DROP INDEX "uq_votes_round_voter"`);
        await queryRunner.query(`DROP TABLE "votes"`);
        await queryRunner.query(`ALTER TABLE "rounds" DROP COLUMN "voting_closed_at"`);
    }
}
```

`down()` 이 `up()` 의 정확한 역순인지 확인할 것 — FK 를 인덱스·테이블보다 먼저 드롭한다.

- [ ] **Step 4: 모듈 등록**

`src/modules/game/game.module.ts` 의 `ENTITIES` 배열에 `Vote` 를 추가한다. 기존 항목(`Room, Participant, Round, Answer`)을 지우지 말 것.

- [ ] **Step 5: 엔티티 테스트**

`src/modules/game/entities/vote.entity.spec.ts`:

```typescript
import { Vote } from './vote.entity';

describe('Vote 엔티티', () => {
  it('필드를 담는다', () => {
    const vote = new Vote();
    vote.roundId = '10';
    vote.voterParticipantId = '2';
    vote.answerId = '77';

    expect(vote.roundId).toBe('10');
    expect(vote.voterParticipantId).toBe('2');
    expect(vote.answerId).toBe('77');
  });
});
```

- [ ] **Step 6: 마이그레이션 적용과 확인**

```bash
npm run typeorm -- migration:run
PGPASSWORD=quizapp psql -h localhost -p 55432 -U postgres -d quiz_app -tAc \
  "select column_name from information_schema.columns where table_name='votes' order by ordinal_position;"
PGPASSWORD=quizapp psql -h localhost -p 55432 -U postgres -d quiz_app -tAc \
  "select indexdef from pg_indexes where tablename='votes';"
```

Expected: 컬럼 5개(`id, round_id, voter_participant_id, answer_id, created_at`), 유니크 인덱스 `uq_votes_round_voter` 존재.

가역성도 확인한다:

```bash
npm run typeorm -- migration:revert
PGPASSWORD=quizapp psql -h localhost -p 55432 -U postgres -d quiz_app -tAc \
  "select count(*) from information_schema.tables where table_name='votes';"
npm run typeorm -- migration:run
```

Expected: revert 후 0, 재적용 후 다시 테이블 존재.

**기존 질문 데이터를 건드리지 않았는지 확인할 것:**

```bash
PGPASSWORD=quizapp psql -h localhost -p 55432 -U postgres -d quiz_app -tAc \
  "select status, count(*) from questions group by status order by 1;"
```

Expected: `live 21` / `pending 17`

- [ ] **Step 7: 커밋**

```bash
npx jest
git add src/modules/game/entities src/infrastructure/database/migrations src/modules/game/game.module.ts
git commit -m "feat: 투표 테이블과 투표 종료 시각 추가"
```

---

## Task 2: 투표 서비스

**레포:** `~/Desktop/quiz-app`

**Files:**
- Create: `src/modules/game/vote.service.ts`
- Modify: `src/modules/game/game.module.ts`
- Test: `src/modules/game/vote.service.spec.ts`

**Interfaces:**
- Consumes: `Vote` (Task 1), `Round.votingClosedAt` (Task 1), `RoomService.listParticipants` (기존)
- Produces:
  - `interface VoteResult { voted: true; allVoted: boolean }`
  - `VoteService.cast(round: Round, participant: Participant, answerId: string): Promise<VoteResult>`
  - `VoteService.close(round: Round): Promise<Round>`
  - `VoteService.myVote(roundId: string, participantId: string): Promise<Vote | null>`
  - `VoteService.countByAnswer(roundId: string): Promise<Map<string, number>>`
  - `VoteService.votedCount(roundId: string): Promise<number>`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/modules/game/vote.service.spec.ts`:

```typescript
import { ConflictException, ForbiddenException, NotFoundException } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { DataSource } from 'typeorm';
import { Answer } from './entities/answer.entity';
import { Participant } from './entities/participant.entity';
import { Round } from './entities/round.entity';
import { Vote } from './entities/vote.entity';
import { RoundStatus } from './enums/round-status.enum';
import { RoomService } from './room.service';
import { VoteService } from './vote.service';

const revealedRound = (over: Partial<Round> = {}): Round =>
  ({ id: '10', roomId: '1', questionId: '42', sequence: 1, status: RoundStatus.REVEALED, votingClosedAt: null, ...over }) as Round;

describe('VoteService', () => {
  let service: VoteService;
  const voteRepo = { find: jest.fn(), findOne: jest.fn(), count: jest.fn() };
  const manager = { findOne: jest.fn(), save: jest.fn(), count: jest.fn() };
  const dataSource = { transaction: jest.fn((cb: (m: typeof manager) => unknown) => cb(manager)) };
  const rooms = { listParticipants: jest.fn() };
  const roundRepo = { findOne: jest.fn(), update: jest.fn() };

  beforeEach(async () => {
    jest.resetAllMocks();
    dataSource.transaction.mockImplementation((cb: (m: typeof manager) => unknown) => cb(manager));
    const moduleRef = await Test.createTestingModule({
      providers: [
        VoteService,
        { provide: getRepositoryToken(Vote), useValue: voteRepo },
        { provide: getRepositoryToken(Round), useValue: roundRepo },
        { provide: DataSource, useValue: dataSource },
        { provide: RoomService, useValue: rooms },
      ],
    }).compile();
    service = moduleRef.get(VoteService);
  });

  describe('cast', () => {
    it('다른 방 참가자면 403', async () => {
      await expect(
        service.cast(revealedRound(), { id: '9', roomId: '999' } as Participant, '77'),
      ).rejects.toBeInstanceOf(ForbiddenException);
    });

    it('공개되지 않은 라운드면 409', async () => {
      manager.findOne.mockResolvedValueOnce(revealedRound({ status: RoundStatus.OPEN }));
      await expect(
        service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77'),
      ).rejects.toBeInstanceOf(ConflictException);
    });

    it('이미 닫힌 투표면 409', async () => {
      manager.findOne.mockResolvedValueOnce(revealedRound({ votingClosedAt: new Date() }));
      await expect(
        service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77'),
      ).rejects.toBeInstanceOf(ConflictException);
    });

    it('그 라운드의 답변이 아니면 404', async () => {
      manager.findOne
        .mockResolvedValueOnce(revealedRound())   // 잠근 라운드
        .mockResolvedValueOnce(null);             // answerId 조회 실패
      await expect(
        service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '999'),
      ).rejects.toBeInstanceOf(NotFoundException);
    });

    it('이미 투표했으면 409', async () => {
      manager.findOne
        .mockResolvedValueOnce(revealedRound())
        .mockResolvedValueOnce({ id: '77', roundId: '10' } as Answer)
        .mockResolvedValueOnce({ id: '5' } as Vote);
      await expect(
        service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77'),
      ).rejects.toBeInstanceOf(ConflictException);
    });

    it('마지막 한 명이 투표하면 그 자리에서 닫힌다', async () => {
      manager.findOne
        .mockResolvedValueOnce(revealedRound())
        .mockResolvedValueOnce({ id: '77', roundId: '10' } as Answer)
        .mockResolvedValueOnce(null);
      rooms.listParticipants.mockResolvedValue([{ id: '1' }, { id: '2' }]);
      manager.count.mockResolvedValue(2);

      const result = await service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77');

      expect(result.allVoted).toBe(true);
      expect(manager.save).toHaveBeenCalledWith(
        Round,
        expect.objectContaining({ votingClosedAt: expect.any(Date) }),
      );
    });

    it('아직 남았으면 닫지 않는다', async () => {
      manager.findOne
        .mockResolvedValueOnce(revealedRound())
        .mockResolvedValueOnce({ id: '77', roundId: '10' } as Answer)
        .mockResolvedValueOnce(null);
      rooms.listParticipants.mockResolvedValue([{ id: '1' }, { id: '2' }, { id: '3' }]);
      manager.count.mockResolvedValue(2);

      const result = await service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77');

      expect(result.allVoted).toBe(false);
      expect(manager.save).not.toHaveBeenCalledWith(Round, expect.anything());
    });

    it('유니크 위반은 409 로 바꾼다', async () => {
      manager.findOne
        .mockResolvedValueOnce(revealedRound())
        .mockResolvedValueOnce({ id: '77', roundId: '10' } as Answer)
        .mockResolvedValueOnce(null);
      manager.save.mockRejectedValueOnce({ driverError: { code: '23505' } });

      await expect(
        service.cast(revealedRound(), { id: '2', roomId: '1' } as Participant, '77'),
      ).rejects.toBeInstanceOf(ConflictException);
    });
  });

  describe('close', () => {
    it('이미 닫혔으면 409', async () => {
      roundRepo.update.mockResolvedValue({ affected: 0 });
      await expect(service.close(revealedRound())).rejects.toBeInstanceOf(ConflictException);
    });

    it('닫으면 시각을 남기고 갱신된 라운드를 돌려준다', async () => {
      roundRepo.update.mockResolvedValue({ affected: 1 });
      roundRepo.findOne.mockResolvedValue(revealedRound({ votingClosedAt: new Date() }));

      const closed = await service.close(revealedRound());

      expect(closed.votingClosedAt).not.toBeNull();
    });
  });

  describe('countByAnswer', () => {
    it('답변별 득표 수를 센다', async () => {
      voteRepo.find.mockResolvedValue([
        { answerId: '77' }, { answerId: '77' }, { answerId: '88' },
      ]);

      const counts = await service.countByAnswer('10');

      expect(counts.get('77')).toBe(2);
      expect(counts.get('88')).toBe(1);
    });

    it('표가 없으면 빈 맵', async () => {
      voteRepo.find.mockResolvedValue([]);
      expect((await service.countByAnswer('10')).size).toBe(0);
    });
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx jest src/modules/game/vote`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

`src/modules/game/vote.service.ts`:

```typescript
import {
  ConflictException,
  ForbiddenException,
  Injectable,
  NotFoundException,
} from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { DataSource, Repository } from 'typeorm';
import { Answer } from './entities/answer.entity';
import { Participant } from './entities/participant.entity';
import { Round } from './entities/round.entity';
import { Vote } from './entities/vote.entity';
import { RoundStatus } from './enums/round-status.enum';
import { RoomService } from './room.service';

const POSTGRES_UNIQUE_VIOLATION = '23505';

export interface VoteResult {
  voted: true;
  allVoted: boolean;
}

@Injectable()
export class VoteService {
  constructor(
    @InjectRepository(Vote) private readonly votes: Repository<Vote>,
    @InjectRepository(Round) private readonly rounds: Repository<Round>,
    private readonly dataSource: DataSource,
    private readonly rooms: RoomService,
  ) {}

  /**
   * 표를 던진다. 답변 제출과 같은 구조다 — 라운드 행을 잠그고 트랜잭션
   * 안에서 상태 확인·중복 확인·insert·완료 판정을 한 번에 처리한다.
   * 잠금이 없으면 닫힌 뒤 들어온 표가 저장되거나 같은 사람의 두 표가 함께
   * 통과한다.
   */
  async cast(
    round: Round,
    participant: Participant,
    answerId: string,
  ): Promise<VoteResult> {
    if (participant.roomId !== round.roomId) {
      throw new ForbiddenException('이 방의 참가자가 아닙니다');
    }

    // 자기 답변에 투표하는 것을 막지 않는다 — 의도된 동작이다.
    // 검사를 "빠뜨린" 것이 아니므로 추가하지 말 것.

    return this.dataSource.transaction<VoteResult>(async (manager) => {
      const locked = await manager.findOne(Round, {
        where: { id: round.id },
        lock: { mode: 'pessimistic_write' },
      });
      if (!locked) throw new NotFoundException(`라운드를 찾을 수 없습니다: ${round.id}`);
      if (locked.status !== RoundStatus.REVEALED) {
        throw new ConflictException('아직 공개되지 않은 라운드입니다');
      }
      if (locked.votingClosedAt !== null) {
        throw new ConflictException('투표가 이미 끝났습니다');
      }

      // 다른 라운드의 답변에 투표하는 것을 막는다.
      const answer = await manager.findOne(Answer, {
        where: { id: answerId, roundId: locked.id },
      });
      if (!answer) {
        throw new NotFoundException('이 라운드에 없는 답변입니다');
      }

      const existing = await manager.findOne(Vote, {
        where: { roundId: locked.id, voterParticipantId: participant.id },
      });
      if (existing) {
        throw new ConflictException('이미 투표했습니다. 수정할 수 없습니다');
      }

      try {
        await manager.save(Vote, {
          roundId: locked.id,
          voterParticipantId: participant.id,
          answerId,
        } as Vote);
      } catch (error) {
        if (
          (error as { driverError?: { code?: string } }).driverError?.code ===
          POSTGRES_UNIQUE_VIOLATION
        ) {
          throw new ConflictException('이미 투표했습니다. 수정할 수 없습니다');
        }
        throw error;
      }

      const [participants, voted] = await Promise.all([
        this.rooms.listParticipants(locked.roomId),
        manager.count(Vote, { where: { roundId: locked.id } }),
      ]);

      const allVoted = voted >= participants.length;
      if (allVoted) {
        locked.votingClosedAt = new Date();
        await manager.save(Round, locked);
      }

      return { voted: true, allVoted };
    });
  }

  /**
   * 방장이 투표를 강제 종료한다. 조건부 UPDATE 라 이미 닫힌 라운드를
   * 다시 닫거나 시각을 덮어쓰지 않는다.
   */
  async close(round: Round): Promise<Round> {
    const result = await this.rounds.update(
      { id: round.id, votingClosedAt: null },
      { votingClosedAt: new Date() },
    );
    if (result.affected === 0) {
      throw new ConflictException('투표가 이미 끝났습니다');
    }
    const updated = await this.rounds.findOne({ where: { id: round.id } });
    if (!updated) throw new NotFoundException(`라운드를 찾을 수 없습니다: ${round.id}`);
    return updated;
  }

  /** 내가 던진 표. 안 던졌으면 null. */
  async myVote(roundId: string, participantId: string): Promise<Vote | null> {
    return this.votes.findOne({ where: { roundId, voterParticipantId: participantId } });
  }

  /**
   * 답변별 득표 수. 누가 던졌는지는 담지 않는다 — 결과 화면에는 수만 나간다.
   */
  async countByAnswer(roundId: string): Promise<Map<string, number>> {
    const rows = await this.votes.find({
      where: { roundId },
      select: { answerId: true },
    });
    const counts = new Map<string, number>();
    for (const row of rows) {
      counts.set(row.answerId, (counts.get(row.answerId) ?? 0) + 1);
    }
    return counts;
  }

  /** 몇 명이 투표했는지. */
  async votedCount(roundId: string): Promise<number> {
    return this.votes.count({ where: { roundId } });
  }
}
```

`game.module.ts` 의 `providers` 와 `exports` 에 `VoteService` 를 추가한다. 기존 항목을 지우지 말 것.

- [ ] **Step 4: 통과 확인**

Run: `npx jest src/modules/game/vote`
Expected: PASS (11 tests)

- [ ] **Step 5: 커밋**

```bash
npx jest
git add src/modules/game/vote.service.ts src/modules/game/vote.service.spec.ts src/modules/game/game.module.ts
git commit -m "feat: 투표 서비스 추가

답변 제출과 같이 라운드 행 잠금 트랜잭션 안에서 처리한다.
잠금이 없으면 닫힌 뒤 들어온 표가 저장되거나 같은 사람의 두 표가
함께 통과한다."
```

---

## Task 3: 답변 길이 제공

**레포:** `~/Desktop/quiz-app`

**Files:**
- Modify: `src/modules/game/round.service.ts`
- Test: `src/modules/game/round.service.spec.ts` (추가)

**Interfaces:**
- Produces: `RoundService.answerLengths(roundId: string): Promise<Map<string, number>>` — 참가자 id → 답변 길이

**왜 필요한가:** 작성 화면의 블러 블록은 길이만 있으면 그릴 수 있다. 텍스트를 클라이언트로 보내지 않는 것이 이 기능의 핵심 제약이므로, 서버가 길이만 계산해 넘긴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/modules/game/round.service.spec.ts` 에 추가:

```typescript
  describe('answerLengths', () => {
    it('참가자별 답변 길이를 돌려준다', async () => {
      answerRepo.find.mockResolvedValue([
        { participantId: '1', text: '짧음' },
        { participantId: '2', text: '조금 더 긴 답변입니다' },
      ]);

      const lengths = await service.answerLengths('10');

      expect(lengths.get('1')).toBe(2);
      expect(lengths.get('2')).toBe(12);
    });

    it('아무도 안 냈으면 빈 맵', async () => {
      answerRepo.find.mockResolvedValue([]);
      expect((await service.answerLengths('10')).size).toBe(0);
    });
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx jest src/modules/game/round`
Expected: FAIL — `answerLengths` 가 함수가 아님

- [ ] **Step 3: 구현**

`round.service.ts` 에 `submittedParticipantIds` 아래로 추가:

```typescript
  /**
   * 참가자별 답변 길이. 공개 전 화면의 블러 블록을 그리는 데 쓴다.
   *
   * 텍스트가 아니라 길이만 내보내는 것이 요점이다. CSS 로 가린 진짜 텍스트는
   * 개발자도구로 읽히므로, 클라이언트에 도달하지 않게 하는 것이 유일한 방어다.
   */
  async answerLengths(roundId: string): Promise<Map<string, number>> {
    const rows = await this.answers.find({
      where: { roundId },
      select: { participantId: true, text: true },
    });
    return new Map(rows.map((row) => [row.participantId, row.text.length]));
  }
```

- [ ] **Step 4: 통과 확인**

Run: `npx jest src/modules/game/round`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
npx jest
git add src/modules/game/round.service.ts src/modules/game/round.service.spec.ts
git commit -m "feat: 참가자별 답변 길이 조회 추가

공개 전 블러 블록용. 텍스트가 아니라 길이만 내보낸다."
```

---

## Task 4: 응답 DTO 확장

**레포:** `~/Desktop/quiz-app`

**Files:**
- Create: `src/modules/game/dto/vote.dto.ts`
- Modify: `src/modules/game/dto/round.dto.ts`
- Modify: `src/modules/game/dto/participant.dto.ts`

**Interfaces:**
- Produces: `CastVoteDto`, `CastVoteResponseDto`, `MyVoteDto`; `ParticipantSubmissionDto.answerLength`; `RevealedAnswerDto.answerId`·`voteCount`; `RoundRevealedResponseDto.myVote`·`votedCount`·`votingClosedAt`

- [ ] **Step 1: 투표 DTO**

`src/modules/game/dto/vote.dto.ts`:

```typescript
import { ApiProperty } from '@nestjs/swagger';
import { IsString } from 'class-validator';

export class CastVoteDto {
  @ApiProperty({ description: '투표할 답변의 id', example: '77' })
  @IsString({ message: '답변 id 가 필요합니다' })
  answerId!: string;
}

export class CastVoteResponseDto {
  @ApiProperty({ description: '항상 true', example: true })
  voted!: boolean;

  @ApiProperty({
    description: 'true 면 이 표로 투표가 끝나 득표 수가 공개됐다',
    example: false,
  })
  allVoted!: boolean;
}

export class MyVoteDto {
  @ApiProperty({ description: '내가 투표한 답변의 id', example: '77' })
  answerId!: string;
}
```

- [ ] **Step 2: 참가자 DTO 에 길이 추가**

`src/modules/game/dto/participant.dto.ts` 의 `ParticipantSubmissionDto` 에 추가:

```typescript
  @ApiProperty({
    description:
      '제출한 답변의 글자 수. 미제출이면 null. 공개 전 블러 블록을 그리는 용도이며 ' +
      '답변 내용은 어떤 경우에도 포함되지 않는다',
    example: 23,
    nullable: true,
  })
  answerLength!: number | null;
```

- [ ] **Step 3: 공개 응답 DTO 확장**

`src/modules/game/dto/round.dto.ts` 의 `RevealedAnswerDto` 에 추가:

```typescript
  @ApiProperty({ description: '이 답변의 id. 투표할 때 이 값을 보낸다', example: '77' })
  answerId!: string;

  @ApiProperty({
    description:
      '득표 수. 투표가 끝나기 전에는 null 이다 — 실시간 집계가 보이면 ' +
      '앞서는 답에 표가 쏠린다',
    example: 2,
    nullable: true,
  })
  voteCount!: number | null;
```

`RoundRevealedResponseDto` 에 추가:

```typescript
  @ApiProperty({
    description: '내가 투표한 답변. 아직 안 했으면 null',
    type: MyVoteDto,
    nullable: true,
  })
  myVote!: MyVoteDto | null;

  @ApiProperty({ description: '투표를 마친 사람 수. 누가 했는지는 포함되지 않는다', example: 2 })
  votedCount!: number;

  @ApiProperty({
    description:
      '투표가 끝난 시각 (ISO 8601). 아직 진행 중이면 null. ' +
      '모든 클라이언트가 이 시각을 기준으로 결과 연출 타이밍을 계산한다',
    example: '2026-09-09T12:35:10.000Z',
    nullable: true,
  })
  votingClosedAt!: string | null;
```

`MyVoteDto` 를 `vote.dto.ts` 에서 import 한다.

- [ ] **Step 4: 타입 확인과 커밋**

```bash
npx tsc --noEmit
```

이 단계에서는 컨트롤러가 새 필드를 아직 채우지 않으므로 타입 오류가 난다. Task 5 에서 채운다. **오류를 없애려고 DTO 필드를 optional 로 바꾸지 말 것** — 필수 필드로 두는 것이 컨트롤러가 채우도록 강제하는 장치다.

```bash
git add src/modules/game/dto
git commit -m "feat: 투표 응답 DTO 추가

voteCount 는 투표가 끝나기 전 null 이다. 실시간 집계가 보이면
앞서는 답에 표가 쏠려 남 눈치 안 보고 고르는 성질이 깨진다."
```

---

## Task 5: 투표 엔드포인트와 응답 조립

**레포:** `~/Desktop/quiz-app`

**Files:**
- Modify: `src/modules/game/game.controller.ts`
- Test: `src/modules/game/game.controller.spec.ts` (추가)

**Interfaces:**
- Consumes: `VoteService` (Task 2), `RoundService.answerLengths` (Task 3), DTO (Task 4)
- Produces: `POST /rounds/:id/votes`, `POST /rounds/:id/votes/close`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/modules/game/game.controller.spec.ts` 에 추가한다. 기존 목 객체에 `votes` 를 더한다:

```typescript
  const votes = {
    cast: jest.fn(),
    close: jest.fn(),
    myVote: jest.fn(),
    countByAnswer: jest.fn(),
    votedCount: jest.fn(),
  };
```

`Test.createTestingModule` 의 `providers` 에 `{ provide: VoteService, useValue: votes }` 를 추가하고, `rounds` 목에 `answerLengths: jest.fn()` 을 더한다.

테스트:

```typescript
  it('투표 전에는 득표 수를 보내지 않는다', async () => {
    const round = {
      id: '11', roomId: '1', questionId: '42', status: RoundStatus.REVEALED,
      revealedAt: new Date(), revealedBy: null, votingClosedAt: null,
    } as Round;
    rounds.findById.mockResolvedValue(round);
    rooms.findById.mockResolvedValue({ id: '1', hostParticipantId: '1' } as Room);
    rooms.listParticipants.mockResolvedValue([
      { id: '1', nickname: '지훈' }, { id: '2', nickname: '민수' },
    ]);
    rounds.listAnswers.mockResolvedValue([
      { id: '77', participantId: '1', text: '내 답' },
      { id: '88', participantId: '2', text: '남의 답' },
    ]);
    questions.findOne.mockResolvedValue({ id: '42', text: '질문?' });
    votes.myVote.mockResolvedValue(null);
    votes.votedCount.mockResolvedValue(1);
    votes.countByAnswer.mockResolvedValue(new Map([['77', 1]]));

    const result = await controller.getRound('11', { id: '1', roomId: '1' } as Participant);

    const revealed = result as RoundRevealedResponseDto;
    expect(revealed.answers.every((a) => a.voteCount === null)).toBe(true);
    expect(revealed.votedCount).toBe(1);
    expect(revealed.votingClosedAt).toBeNull();
  });

  it('투표가 끝나면 득표 수가 실린다', async () => {
    const closedAt = new Date();
    const round = {
      id: '11', roomId: '1', questionId: '42', status: RoundStatus.REVEALED,
      revealedAt: new Date(), revealedBy: null, votingClosedAt: closedAt,
    } as Round;
    rounds.findById.mockResolvedValue(round);
    rooms.findById.mockResolvedValue({ id: '1', hostParticipantId: '1' } as Room);
    rooms.listParticipants.mockResolvedValue([
      { id: '1', nickname: '지훈' }, { id: '2', nickname: '민수' },
    ]);
    rounds.listAnswers.mockResolvedValue([
      { id: '77', participantId: '1', text: '내 답' },
      { id: '88', participantId: '2', text: '남의 답' },
    ]);
    questions.findOne.mockResolvedValue({ id: '42', text: '질문?' });
    votes.myVote.mockResolvedValue({ answerId: '88' });
    votes.votedCount.mockResolvedValue(2);
    votes.countByAnswer.mockResolvedValue(new Map([['88', 2]]));

    const revealed = (await controller.getRound(
      '11', { id: '1', roomId: '1' } as Participant,
    )) as RoundRevealedResponseDto;

    expect(revealed.answers.find((a) => a.answerId === '88')?.voteCount).toBe(2);
    expect(revealed.answers.find((a) => a.answerId === '77')?.voteCount).toBe(0);
    expect(revealed.myVote).toEqual({ answerId: '88' });
    expect(revealed.votingClosedAt).toBe(closedAt.toISOString());
  });

  it('공개 전 응답에 답변 길이는 있고 텍스트는 없다', async () => {
    const round = {
      id: '11', roomId: '1', questionId: '42', status: RoundStatus.OPEN,
    } as Round;
    rounds.findById.mockResolvedValue(round);
    rooms.findById.mockResolvedValue({ id: '1', hostParticipantId: '1' } as Room);
    rooms.listParticipants.mockResolvedValue([
      { id: '1', nickname: '지훈' }, { id: '2', nickname: '민수' },
    ]);
    rounds.submittedParticipantIds.mockResolvedValue(new Set(['2']));
    rounds.findMyAnswer.mockResolvedValue(null);
    rounds.answerLengths.mockResolvedValue(new Map([['2', 9]]));
    questions.findOne.mockResolvedValue({ id: '42', text: '질문?' });

    const result = await controller.getRound('11', { id: '1', roomId: '1' } as Participant);

    const serialized = JSON.stringify(result);
    expect(serialized).not.toContain('남의 답');
    const open = result as RoundOpenResponseDto;
    expect(open.participants.find((p) => p.id === '2')?.answerLength).toBe(9);
    expect(open.participants.find((p) => p.id === '1')?.answerLength).toBeNull();
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx jest src/modules/game/game.controller`
Expected: FAIL

- [ ] **Step 3: 공개 전 응답에 길이 싣기**

`getRound` 의 열린 분기에서 `answerLengths` 를 함께 가져와 참가자 항목에 넣는다:

```typescript
    if (round.status === RoundStatus.OPEN) {
      const [submitted, mine, lengths] = await Promise.all([
        this.rounds.submittedParticipantIds(round.id),
        this.rounds.findMyAnswer(round.id, me.id),
        this.rounds.answerLengths(round.id),
      ]);
      return {
        roundId: round.id,
        status: 'open',
        question: questionDto,
        participants: participants.map((p) => ({
          id: p.id,
          nickname: p.nickname,
          submitted: submitted.has(p.id),
          answerLength: lengths.get(p.id) ?? null,
        })),
        mySubmission: mine ? { text: mine.text } : null,
      };
    }
```

- [ ] **Step 4: 공개 응답에 투표 상태 싣기**

공개 분기에서 투표 정보를 조립한다. **`votingClosedAt` 이 `null` 이면 `voteCount` 를 `null` 로 둔다:**

```typescript
    const answers = await this.rounds.listAnswers(round);
    const [myVote, votedCount, voteCounts] = await Promise.all([
      this.votes.myVote(round.id, me.id),
      this.votes.votedCount(round.id),
      this.votes.countByAnswer(round.id),
    ]);
    const votingClosed = round.votingClosedAt !== null;

    return {
      // ... 기존 필드 그대로 ...
      answers: order.map((a) => ({
        participantId: a.participantId,
        nickname: byId.get(a.participantId)?.nickname ?? '(알 수 없음)',
        text: a.text,
        answerId: a.id,
        // 투표 중에는 집계를 내보내지 않는다. 보이면 앞서는 답에 표가 쏠린다.
        voteCount: votingClosed ? (voteCounts.get(a.id) ?? 0) : null,
      })),
      myVote: myVote ? { answerId: myVote.answerId } : null,
      votedCount,
      votingClosedAt: round.votingClosedAt ? round.votingClosedAt.toISOString() : null,
    };
```

- [ ] **Step 5: 엔드포인트 두 개**

```typescript
  @Post('rounds/:id/votes')
  @UseGuards(ParticipantGuard)
  @ApiSecurity('participantToken')
  @ApiOperation({
    summary: '투표',
    description: `공개된 답변 중 하나에 투표한다. **1인 1표이고 수정할 수 없다.** 자기 답변에도 투표할 수 있다.

\`\`\`jsonc
// 요청
{ "answerId": "77" }

// 응답 201
{
  "voted": true,
  "allVoted": false      // true 면 이 표로 투표가 끝나 득표 수가 공개됐다
}
\`\`\`

투표 중에는 \`GET /rounds/:id\` 의 \`answers[].voteCount\` 가 모두 \`null\` 이다. 투표가 끝나야 실제 수가 들어온다.

| 상태 | 이유 |
| --- | --- |
| 403 | 이 방 참가자가 아님 |
| 404 | 이 라운드에 없는 answerId |
| 409 | 아직 공개되지 않음 / 이미 투표함 / 투표가 이미 끝남 |`,
  })
  @ApiCreatedResponse({ type: CastVoteResponseDto })
  async castVote(
    @Param('id') id: string,
    @Body() dto: CastVoteDto,
    @CurrentParticipant() me: Participant,
  ): Promise<CastVoteResponseDto> {
    const round = await this.rounds.findById(id);
    const result = await this.votes.cast(round, me, dto.answerId);
    return { voted: result.voted, allVoted: result.allVoted };
  }

  @Post('rounds/:id/votes/close')
  @UseGuards(ParticipantGuard)
  @ApiSecurity('participantToken')
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: '투표 강제 종료',
    description: `아직 투표하지 않은 사람이 있어도 투표를 닫고 득표 수를 공개한다. **방장만 호출할 수 있다.**

응답은 갱신된 라운드 상태이며, \`votingClosedAt\` 이 채워지고 \`answers[].voteCount\` 에 실제 수가 들어온다.

| 상태 | 이유 |
| --- | --- |
| 403 | 방장이 아님 |
| 409 | 투표가 이미 끝남 |`,
  })
  @ApiOkResponse({ type: RoundRevealedResponseDto })
  async closeVoting(
    @Param('id') id: string,
    @CurrentParticipant() me: Participant,
  ): Promise<RoundRevealedResponseDto> {
    const round = await this.rounds.findById(id);
    const room = await this.roomOf(round.roomId);
    this.rooms.assertMember(room, me);
    this.rooms.assertHost(room, me);
    await this.votes.close(round);
    return (await this.getRound(id, me)) as RoundRevealedResponseDto;
  }
```

생성자에 `private readonly votes: VoteService` 를 추가한다.

**`castVote` 에 `assertMember` 를 두지 않는 이유:** `VoteService.cast` 가 `participant.roomId !== round.roomId` 를 직접 확인한다. 답변 제출과 같은 구조다 — 불변식을 지키는 서비스가 스스로 검증한다.

- [ ] **Step 6: 통과 확인**

```bash
npx jest src/modules/game/game.controller
npx tsc --noEmit
```

Expected: PASS, 타입 오류 없음

- [ ] **Step 7: swagger 확인**

`SwaggerModule.createDocument` 를 직접 호출하는 일회성 스크립트로 문서를 덤프해, 새 엔드포인트 두 개가 응답 스키마를 갖는지 확인한다. **`npm run start:dev` + curl 로 하지 말 것** — 백그라운드 서버에 물려 멈추기 쉽다. 확인 후 스크립트는 지운다.

Expected: `POST /rounds/{id}/votes` 와 `POST /rounds/{id}/votes/close` 가 `content` 를 가진다.

- [ ] **Step 8: 커밋**

```bash
npx jest
git add src/modules/game
git commit -m "feat: 투표 엔드포인트와 응답 조립

투표 중에는 voteCount 를 null 로 보낸다. 공개 전 응답에는
답변 길이만 싣고 텍스트는 싣지 않는다."
```

---

## Task 6: 프론트 타입 확장

**레포:** `~/Desktop/quiz-app-web`

**Files:**
- Modify: `lib/types.ts`

**Interfaces:**
- Produces: `ParticipantSubmission.answerLength`, `RevealedAnswer.answerId`·`voteCount`, `RoundRevealed.myVote`·`votedCount`·`votingClosedAt`, `CastVoteResult`

- [ ] **Step 1: 타입 추가**

`lib/types.ts` 를 수정한다:

```typescript
export interface ParticipantSubmission {
  id: string;
  nickname: string;
  submitted: boolean;
  // 제출한 답변의 글자 수. 미제출이면 null.
  // 블러 블록을 그리는 용도이며, 답변 텍스트는 이 타입 어디에도 없다.
  answerLength: number | null;
}

export interface RevealedAnswer {
  participantId: string;
  nickname: string;
  text: string;
  answerId: string;
  // 투표가 끝나기 전에는 null.
  voteCount: number | null;
}

export interface RoundRevealed {
  roundId: string;
  status: 'revealed';
  question: Question;
  revealedAt: string | null;
  revealedBy: Participant | null;
  answers: RevealedAnswer[];
  notSubmitted: Participant[];
  myVote: { answerId: string } | null;
  votedCount: number;
  votingClosedAt: string | null;
}

export interface CastVoteResult {
  voted: boolean;
  allVoted: boolean;
}
```

`RoundOpen` 은 그대로다 — `participants` 의 타입이 바뀌면서 자동으로 반영된다.

- [ ] **Step 2: 확인**

```bash
npx tsc --noEmit
```

기존 컴포넌트가 `answerLength` 를 채우지 않는 목 객체를 쓰고 있으면 여기서 오류가 난다. **타입을 optional 로 바꾸지 말고** 해당 테스트 픽스처에 `answerLength: null` 을 채운다.

```bash
npm test
```

- [ ] **Step 3: 커밋**

```bash
git add lib/types.ts
git commit -m "feat: 투표·블러 관련 응답 타입 추가"
```

---

## Task 7: 블러 컴포넌트

**레포:** `~/Desktop/quiz-app-web`

**Files:**
- Create: `components/game/BlurredAnswer.tsx`
- Test: `components/game/BlurredAnswer.test.tsx`

**Interfaces:**
- Produces: `<BlurredAnswer length={number} />`

**이 컴포넌트는 텍스트를 받지 않는다.** props 에 문자열이 없다는 것이 이 기능의 방어선이다.

- [ ] **Step 1: 실패하는 테스트 작성**

`components/game/BlurredAnswer.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { BlurredAnswer } from './BlurredAnswer';

describe('BlurredAnswer', () => {
  it('길이에 비례해 블록을 그린다', () => {
    const short = render(<BlurredAnswer length={5} />);
    const shortBlocks = short.container.querySelectorAll('[data-blur-block]').length;
    short.unmount();

    const long = render(<BlurredAnswer length={80} />);
    const longBlocks = long.container.querySelectorAll('[data-blur-block]').length;

    expect(longBlocks).toBeGreaterThan(shortBlocks);
  });

  it('길이가 0이어도 죽지 않는다', () => {
    expect(() => render(<BlurredAnswer length={0} />)).not.toThrow();
  });

  it('아주 긴 답변도 블록 수가 무한정 늘지 않는다', () => {
    const { container } = render(<BlurredAnswer length={500} />);
    expect(container.querySelectorAll('[data-blur-block]').length).toBeLessThanOrEqual(12);
  });

  it('렌더된 DOM 에 읽을 수 있는 텍스트가 없다', () => {
    const { container } = render(<BlurredAnswer length={40} />);
    expect(container.textContent?.trim()).toBe('');
  });
});
```

마지막 테스트가 이 컴포넌트의 존재 이유다. 텍스트가 들어오면 실패한다.

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run components/game/BlurredAnswer`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

`components/game/BlurredAnswer.tsx`:

```tsx
'use client';

const CHARS_PER_BLOCK = 8;
const MAX_BLOCKS = 12;

export interface BlurredAnswerProps {
  length: number;
}

/**
 * 아직 공개되지 않은 답변 자리에 놓이는 가짜 블록.
 *
 * 이 컴포넌트는 답변 텍스트를 받지 않는다. 실제 텍스트를 CSS 로 흐리게
 * 만드는 구현은 개발자도구로 읽히므로, 애초에 텍스트를 클라이언트로
 * 가져오지 않는 것이 유일한 방어다. 서버는 길이만 보낸다.
 */
export function BlurredAnswer({ length }: BlurredAnswerProps) {
  const count = Math.min(MAX_BLOCKS, Math.max(1, Math.ceil(length / CHARS_PER_BLOCK)));
  const blocks = Array.from({ length: count }, (_, i) => i);

  return (
    <div aria-label="아직 볼 수 없는 답변" className="flex flex-wrap gap-1.5">
      {blocks.map((i) => (
        <span
          key={i}
          data-blur-block
          aria-hidden="true"
          className="h-3 rounded-full bg-zinc-300/70 dark:bg-zinc-700/70"
          style={{ width: `${28 + ((i * 17) % 36)}px` }}
        />
      ))}
    </div>
  );
}
```

블록 너비를 인덱스로 흔드는 것은 자연스럽게 보이기 위한 것이다. 난수를 쓰면 리렌더마다 모양이 바뀐다.

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run components/game/BlurredAnswer`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
npm test && npx tsc --noEmit
git add components/game/BlurredAnswer.tsx components/game/BlurredAnswer.test.tsx
git commit -m "feat: 블러 답변 블록 컴포넌트 추가

텍스트를 받지 않는다. CSS 로 가린 진짜 텍스트는 개발자도구로
읽히므로 애초에 클라이언트로 가져오지 않는 것이 유일한 방어다."
```

---

## Task 8: 작성·대기 화면에 블러 목록

**레포:** `~/Desktop/quiz-app-web`

**Files:**
- Modify: `components/game/AnswerForm.tsx`
- Modify: `components/game/Waiting.tsx`
- Test: `components/game/Waiting.test.tsx` (추가)

**Interfaces:**
- Consumes: `BlurredAnswer` (Task 7), `ParticipantSubmission.answerLength` (Task 6)

- [ ] **Step 1: 실패하는 테스트 작성**

`components/game/Waiting.test.tsx` 에 추가한다. 기존 픽스처의 참가자에 `answerLength` 를 채운다:

```typescript
  it('제출한 사람 자리에 블러 블록이 놓인다', () => {
    render(<Waiting round={round} isHost={false} onReveal={noop} onSkip={noop} busy={false} />);
    expect(screen.getAllByLabelText('아직 볼 수 없는 답변').length).toBeGreaterThan(0);
  });

  it('미제출자 자리에는 블러 블록이 없다', () => {
    const onlyPending = {
      ...round,
      participants: [{ id: '3', nickname: '영희', submitted: false, answerLength: null }],
      mySubmission: { text: '내가 쓴 답' },
    };
    render(<Waiting round={onlyPending} isHost={false} onReveal={noop} onSkip={noop} busy={false} />);
    expect(screen.queryByLabelText('아직 볼 수 없는 답변')).toBeNull();
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run components/game/Waiting`
Expected: FAIL

- [ ] **Step 3: 구현**

`Waiting.tsx` 의 참가자 목록에서, `submitted` 이고 **본인이 아닌** 참가자 아래에 `<BlurredAnswer length={p.answerLength ?? 0} />` 를 렌더한다. 본인 자리에는 이미 내 답변이 그대로 보이므로 블록을 놓지 않는다.

`AnswerForm.tsx` 에도 같은 목록을 추가한다. 아직 내 답을 쓰는 중이므로 내 자리는 비어 있고, 남들이 낸 자리에만 블록이 쌓인다.

두 화면 모두 기존 제출 여부 표시(체크·대기)는 그대로 둔다.

- [ ] **Step 4: 통과 확인**

```bash
npm test
npx tsc --noEmit
```

- [ ] **Step 5: 유출 확인**

브라우저에서 3명 방을 만들어 2명만 제출한 뒤, 미제출자 시점으로 화면을 열고 **DOM 에 남의 답변 텍스트가 없는지** 확인한다:

```javascript
document.documentElement.outerHTML.includes('<남이 쓴 답변 문자열>')
```

Expected: `false`

**확인 후 만든 방·참가자·라운드·답변을 전부 지울 것** (`rooms.host_participant_id` 를 먼저 NULL 로).

- [ ] **Step 6: 커밋**

```bash
git add components/game
git commit -m "feat: 작성·대기 화면에 블러 답변 블록 추가"
```

---

## Task 9: 투표 화면

**레포:** `~/Desktop/quiz-app-web`

**Files:**
- Modify: `components/game/Reveal.tsx`
- Modify: `app/room/[code]/page.tsx`
- Test: `components/game/Reveal.test.tsx` (추가)

**Interfaces:**
- Consumes: `RoundRevealed` (Task 6), `revealTimings`·`STAGGER_MS` (기존 `@/lib/reveal`), `seededShuffle` (기존 `@/lib/shuffle`)

- [ ] **Step 1: 실패하는 테스트 작성**

`components/game/Reveal.test.tsx` 에 추가한다. 기존 픽스처에 `myVote: null, votedCount: 0, votingClosedAt: null` 과 각 답변의 `answerId`, `voteCount: null` 을 채운다.

```typescript
  it('투표 전에는 득표 수를 표시하지 않는다', () => {
    vi.setSystemTime(Date.parse(REVEALED_AT) + 10_000);
    render(<Reveal round={round} isHost={false} onNext={noop} onVote={noop} onCloseVoting={noop} busy={false} />);
    expect(screen.queryByText(/표$/)).toBeNull();
  });

  it('투표 전에는 다음 질문 버튼이 없다', () => {
    vi.setSystemTime(Date.parse(REVEALED_AT) + 10_000);
    render(<Reveal round={round} isHost onNext={noop} onVote={noop} onCloseVoting={noop} busy={false} />);
    expect(screen.queryByRole('button', { name: /다음 질문/ })).toBeNull();
  });

  it('투표하면 내가 고른 답이 표시된다', () => {
    vi.setSystemTime(Date.parse(REVEALED_AT) + 10_000);
    const voted = { ...round, myVote: { answerId: '77' }, votedCount: 1 };
    render(<Reveal round={voted} isHost={false} onNext={noop} onVote={noop} onCloseVoting={noop} busy={false} />);
    expect(screen.getByLabelText('내가 고른 답')).toBeInTheDocument();
  });

  it('투표가 끝나면 득표 수와 다음 질문 버튼이 나온다', () => {
    const closedAt = new Date(Date.parse(REVEALED_AT) + 5_000).toISOString();
    vi.setSystemTime(Date.parse(closedAt) + 10_000);
    const closed = {
      ...round,
      votingClosedAt: closedAt,
      myVote: { answerId: '77' },
      votedCount: 2,
      answers: round.answers.map((a, i) => ({ ...a, voteCount: i === 0 ? 2 : 0 })),
    };
    render(<Reveal round={closed} isHost onNext={noop} onVote={noop} onCloseVoting={noop} busy={false} />);
    expect(screen.getByText('2표')).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /다음 질문/ })).toBeInTheDocument();
  });

  it('방장이 아니면 강제 종료 버튼이 없다', () => {
    vi.setSystemTime(Date.parse(REVEALED_AT) + 10_000);
    render(<Reveal round={round} isHost={false} onNext={noop} onVote={noop} onCloseVoting={noop} busy={false} />);
    expect(screen.queryByRole('button', { name: /결과 보기/ })).toBeNull();
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run components/game/Reveal`
Expected: FAIL

- [ ] **Step 3: 구현**

`Reveal.tsx` 의 props 에 `onVote: (answerId: string) => void` 와 `onCloseVoting: () => void` 를 추가하고, 세 모습을 만든다:

- **투표 전** (`votingClosedAt === null && myVote === null`): 각 카드에 투표 버튼. 하단에 "N명 중 M명 투표". 방장에게 "그냥 결과 보기".
- **투표함** (`votingClosedAt === null && myVote !== null`): 내가 고른 카드에 `aria-label="내가 고른 답"` 표시. 나머지는 대기. 방장 버튼 유지.
- **투표 끝** (`votingClosedAt !== null`): 각 카드에 `{voteCount}표`. 방장에게 "다음 질문".

**득표 수 연출은 기존 `revealTimings` 를 재사용한다.** 답변 카드가 `revealedAt` 을 기준으로 열리는 것과 같은 방식으로, 득표 수는 `votingClosedAt` 을 기준으로 붙는다:

```typescript
const [mountedAt] = useState(() => Date.now());

const voteTimings = useMemo(
  () => revealTimings(round.votingClosedAt, mountedAt, order.length),
  [round.votingClosedAt, mountedAt, order.length],
);
```

`useState` 지연 초기화를 쓴다 — `useMemo` 안에서 `Date.now()` 를 부르면 `react-hooks/purity` 에 걸리고 마운트 1회 보장도 약하다.

**카드 순서는 답변 공개 때의 셔플 순서를 그대로 유지한다.** `seededShuffle(round.answers, round.roundId)` 는 이미 결정적이므로 결과 단계에서도 같은 순서가 나온다. 득표 순으로 재정렬하지 않는다 — 방금 읽은 카드를 다시 찾게 된다.

`app/room/[code]/page.tsx` 에 뮤테이션 두 개를 추가한다. 기존 `revealRound`·`skipRound` 와 같은 모양으로, `onSuccess` 에서 라운드 쿼리를 무효화한다:

```typescript
const castVote = useMutation({
  mutationFn: (answerId: string) =>
    apiFetch<CastVoteResult>(`/rounds/${roundId}/votes`, {
      method: 'POST', body: { answerId }, token,
    }),
  onSuccess: () => { void queryClient.invalidateQueries({ queryKey: ['round', roundId] }); },
});
```

`closeVoting` 도 같은 모양으로 `/rounds/${roundId}/votes/close` 를 호출한다.

- [ ] **Step 4: 통과 확인**

```bash
npm test
npx tsc --noEmit
npm run build
```

- [ ] **Step 5: 실제 화면 확인**

로컬 백엔드(`PORT=3001`)와 프론트를 띄우고, 3명 방을 만들어 전원 제출 → 공개 → 투표 → 결과까지 진행한다. 확인할 것:

- 투표 전에 득표 수가 **어디에도** 보이지 않는다 (DOM 검사 포함)
- 투표 후 내가 고른 카드가 표시된다
- 전원 투표하면 득표 수가 순차적으로 붙는다
- 결과의 카드 순서가 공개 때와 같다

**확인 후 만든 데이터를 전부 지울 것** — 방·참가자·라운드·답변·투표·`question_stats`.

- [ ] **Step 6: 커밋**

```bash
git add components/game/Reveal.tsx components/game/Reveal.test.tsx "app/room/[code]/page.tsx"
git commit -m "feat: 투표 화면 추가

득표 수 연출은 답변 공개에 쓰는 revealTimings 를 그대로 쓴다.
votingClosedAt 기준이라 폴링 시점이 달라도 모두 같은 순간에 열린다."
```

---

## 완료 기준

**백엔드** (`~/Desktop/quiz-app`)

```bash
npx jest          # 전부 통과
npm run test:game # e2e 통과
```

**프론트엔드** (`~/Desktop/quiz-app-web`)

```bash
npm test
npx tsc --noEmit
npm run build
```

그리고 실제 브라우저에서:

1. 2명이 제출한 상태에서 미제출자 화면의 **DOM 에 남의 답변 텍스트가 없다**
2. 투표 중 **DOM 에 득표 수가 없다**
3. 전원 투표 시 득표 수가 다 같이 열린다
4. 결과의 카드 순서가 공개 때와 같다

1번과 2번이 이 기능의 존재 이유다.
